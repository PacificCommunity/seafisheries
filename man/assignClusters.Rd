% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clustering.R
\name{assignClusters}
\alias{assignClusters}
\title{Assign observations to the nearest cluster centroid}
\usage{
assignClusters(df, centroids)
}
\arguments{
\item{df}{A dataframe of PCA scores (rows = observations). Column names
must match those of \code{centroids}.}

\item{centroids}{A dataframe of cluster centroids (rows = clusters, columns
= PCA dimensions), typically \code{as.data.frame(kmeans_res$centers)}.}
}
\value{
An integer vector of length \code{nrow(df)} giving the cluster index
(1-based row of the nearest centroid) for each observation.
}
\description{
For each row in \code{df}, computes the Euclidean distance to every centroid and
returns the index of the closest one. Used to apply cluster labels from a
training subset to the full dataset.
}
\examples{
\dontrun{
EC_clean$cluster <- assignClusters(as.data.frame(pca_full),
                                   as.data.frame(kmeans_full$centers))
}

}
\seealso{
Other clustering utilities: 
\code{\link{customKmeans}()},
\code{\link{myPCA}()},
\code{\link{orderClusters}()},
\code{\link{pcaScale}()},
\code{\link{selectScenario}()},
\code{\link{summaryClusters}()}
}
\concept{clustering utilities}
