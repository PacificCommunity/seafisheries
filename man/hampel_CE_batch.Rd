% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fisheries_data.R
\name{hampel_CE_batch}
\alias{hampel_CE_batch}
\title{Hampel filter for catch/effort CPUE, applied per fishery}
\usage{
hampel_CE_batch(df, year_window, radius_km, k, mad_scale = 1.4826)
}
\arguments{
\item{df}{CPUE>0 subset for one fishery, with columns \code{date}, \code{lat},
\code{lon}, \code{CPUE} (same as \code{data_CPUEpos} inside \code{\link[=apply_hampel]{apply_hampel()}}).}

\item{year_window}{Numeric; +/- years defining the time window.}

\item{radius_km}{Numeric; spatial radius in km.}

\item{k}{Numeric; number of (scaled) MADs defining the outlier threshold.}

\item{mad_scale}{Numeric; MAD-to-SD scaling constant. Default 1.4826
(standard constant for consistency with a normal distribution).}
}
\value{
\code{df} with \code{newvalue} (capped CPUE), \code{n_points} (reference points
found), and \code{mad} (scaled MAD used) columns added.
}
\description{
For each row, compares its CPUE against the median/MAD of same-month
CPUE values within \code{year_window} years and \code{radius_km} kilometers of it,
among the rows in \code{df}, and caps CPUE from above at
\verb{median + k * (mad_scale-weighted MAD)} if it exceeds that. Only rows
with at least 3 reference points (in both the time window and the radius)
get a computed cap; others pass through unchanged with \code{n_points}/\code{mad}
left NA.
}
\details{
Precomputes the time-window reference subset once per unique (yr, mm)
pair present in \code{df}. Distances are still computed one query point at a time
against the (much smaller, precomputed) reference subset. Used internally
by \code{\link[=apply_hampel]{apply_hampel()}} (\code{type = "CE"}); exported so it can be run directly.
}
\seealso{
Other hampel filter:
\code{\link[=apply_hampel]{apply_hampel()}},
\code{\link[=calculate_distance]{calculate_distance()}},
\code{\link[=hampel_LF]{hampel_LF()}}
}
\concept{hampel filter}
