% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{plot_fisheries_map}
\alias{plot_fisheries_map}
\title{Plot a gridded fisheries map}
\usage{
plot_fisheries_map(
  df,
  fill_col,
  lat_col = "latCent",
  lon_col = "lonCent",
  facet_col = NULL,
  fill_label = fill_col,
  filter_positive = TRUE,
  filter_sup_quantile = NULL,
  tile_width = 1,
  tile_height = 1
)
}
\arguments{
\item{df}{Data frame containing the data to plot.}

\item{fill_col}{Character. Name of the column in \code{df} to map to tile fill.}

\item{lat_col}{Character. Name of the latitude column. Default \code{"latCent"}.}

\item{lon_col}{Character. Name of the longitude column. Default \code{"lonCent"}.}

\item{facet_col}{Character or NULL. Column to facet by. Default NULL (no facetting).}

\item{fill_label}{Character. Legend title for the fill scale. Defaults to \code{fill_col}.}

\item{filter_positive}{Logical. If TRUE, drop rows where \code{fill_col} is NA or <= 0. Default TRUE.}

\item{filter_sup_quantile}{Numeric or NULL. If not NULL, caps \code{fill_col}
at this upper quantile (values above it are set to the quantile value)
before plotting. Default NULL (no capping).}

\item{tile_width}{Numeric. Tile width in degrees. Default 1.}

\item{tile_height}{Numeric. Tile height in degrees. Default 1.}
}
\description{
Wraps map_bg(), coord_labels(), coord_fixed(), scale_fill_viridis_c(), and
customTheme() into one call.
}
\details{
Tile sizing assumes a uniform grid (fine for EC_clean's 1x1 grid). LF data
isn't uniform -- cell size varies by ASTRAT (2.5 to 10 degrees) -- so a
single tile_width/tile_height is only an approximation there. Facet by
ASTRAT if you need visually accurate cell sizes for LF.

Requires the global objects \code{lon_range} and \code{lat_range} to be defined in
the environment (used to set \code{coord_fixed()} limits).
}
