% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clustering.R
\name{customKmeans}
\alias{customKmeans}
\title{Run k-means clustering with gap statistic to determine optimal K}
\usage{
customKmeans(
  data_df,
  max_k = 15,
  random_set = 100,
  iter_max = 10,
  nstart = 1,
  d.power = 2,
  print_it = TRUE
)
}
\arguments{
\item{data_df}{A numeric dataframe or matrix of PCA scores (rows =
observations, columns = retained principal components).}

\item{max_k}{Integer; maximum number of clusters to evaluate. Default is 15.}

\item{random_set}{Integer; number of bootstrap samples for the gap
statistic (\code{B} in \code{\link[cluster:clusGap]{cluster::clusGap()}}). Default is 100.}

\item{iter_max}{Integer; maximum iterations passed to \code{\link[=kmeans]{kmeans()}}. Default
is 10.}

\item{nstart}{Integer; number of random starts passed to \code{\link[=kmeans]{kmeans()}}.
Default is 1.}

\item{d.power}{Numeric; power of the Euclidean distance used in the gap
statistic computation. Default is 2 (squared Euclidean).}

\item{print_it}{Logical; if \code{TRUE}, prints the gap statistic plot. Default
is \code{TRUE}.}
}
\value{
A named list with three elements:
\describe{
\item{\code{kmeans}}{A \code{kmeans} object fit at the optimal K.}
\item{\code{gap_stat}}{A dataframe with columns \code{logW}, \code{E.logW}, \code{gap}, and
\code{SE.sim} for k = 1 to \code{max_k}.}
\item{\code{plot}}{A ggplot2 object showing the gap statistic vs K with error
bars and a vertical line at the selected K.}
}
}
\description{
Uses \code{\link[cluster:clusGap]{cluster::clusGap()}} with the Tibshirani 2001 SEmax criterion to select
the optimal number of clusters, then runs k-means with that K. Returns the
k-means result, the gap statistic table, and a diagnostic plot.
}
\examples{
\dontrun{
res <- customKmeans(pca_scores, max_k = 10, random_set = 50, print_it = FALSE)
res$kmeans$cluster  # cluster assignments
res$gap_stat        # gap statistic table
res$plot            # diagnostic plot
}

}
\seealso{
Other clustering utilities: 
\code{\link{assignClusters}()},
\code{\link{myPCA}()},
\code{\link{orderClusters}()},
\code{\link{pcaScale}()},
\code{\link{selectScenario}()},
\code{\link{summaryClusters}()}
}
\concept{clustering utilities}
