% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fisheries_data.R
\name{apply_hampel}
\alias{apply_hampel}
\title{Apply a Hampel filter per fishery, for catch/effort or length-frequency data}
\usage{
apply_hampel(
  df,
  type,
  year_window = NULL,
  radius_km = NULL,
  month_window = NULL,
  k,
  mad_scale = 1.4826
)
}
\arguments{
\item{df}{A dataframe (see column requirements above, by \code{type}).}

\item{type}{Character; "CE" or "LF".}

\item{year_window}{Numeric; CE only, +/- years defining the time window.}

\item{radius_km}{Numeric; CE only, spatial radius in km.}

\item{month_window}{Numeric; LF only, +/- months defining the time
window.}

\item{k}{Numeric; number of (scaled) MADs defining the outlier
threshold, both types.}

\item{mad_scale}{Numeric; MAD-to-SD scaling constant. Default 1.4826
(standard constant for consistency with a normal distribution).}
}
\value{
A dataframe: for "CE", \code{df} with \code{newvalue}, \code{n_points}, \code{mad},
\code{adjusted_E}, \code{is_adjusted} columns added (no rows removed). For "LF",
\code{df} (at region x date x len resolution) with outlier region/date
combinations removed.
}
\description{
Loops over each fishery (\code{f}) in \code{df} and runs \code{\link[=hampel_CE_batch]{hampel_CE_batch()}} (for
\code{type = "CE"}) or \code{\link[=hampel_LF_batch]{hampel_LF_batch()}} (for
\code{type = "LF"}), printing a running summary per fishery as it goes.
}
\details{
\code{type = "CE"} expects \code{df} to have \code{f}, \code{yr}, \code{mm}, \code{lat}, \code{lon}, \code{E},
\code{C}, and \code{CPUE} (compute \code{CPUE = C / E} beforehand).
Caps CPUE from above per \code{\link[=hampel_CE_batch]{hampel_CE_batch()}}, then
backs out an adjusted effort (\code{adjusted_E = C / newvalue}) holding catch
fixed. This assumes catch is more reliable than effort when the two
disagree.

\code{type = "LF"} expects \code{df} to have \code{f}, \code{yr}, \code{mm}, \code{lon_from}, \code{lon_to},
\code{lat_from}, \code{lat_to}, \code{len}, \code{count}.

Performance note: each row/group independently re-filters and re-scans
\code{ref_data}, giving O(n) work per row and O(n^2) overall per fishery.
Not restructured here, fine at moderate size, may be slow for very
large fisheries.
}
\seealso{
Other hampel filter:
\code{\link[=calculate_distance]{calculate_distance()}},
\code{\link[=hampel_CE_batch]{hampel_CE_batch()}},
\code{\link[=hampel_LF_batch]{hampel_LF_batch()}}
}
\concept{hampel filter}
