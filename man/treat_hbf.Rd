% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fisheries_data.R
\name{treat_hbf}
\alias{treat_hbf}
\title{Flag and clean Hooks Between Floats (HBF) values}
\usage{
treat_hbf(df, hbf_col = "hbf", threshold = 55)
}
\arguments{
\item{df}{A dataframe.}

\item{hbf_col}{Character; name of the HBF column. Default "hbf".}

\item{threshold}{Numeric; HBF values above this are treated as
implausible and set to NA. Default 55 -- set empirically: values up
to ~52 form a fleet-consistent cluster (repeated by the same vessel),
while values from 60 upward are scattered across unrelated
vessels/flags with no repetition, consistent with data-entry error.
Re-check with \code{\link[=plot_hbf_histogram]{plot_hbf_histogram()}}, faceted by vessel, against new
data periodically -- a fixed ceiling can eventually clip a genuinely
new deep-set fleet practice, the same failure mode this replaced.}
}
\value{
\code{df} with \code{hbf_col} values <= 0 or > \code{threshold} converted to
NA, and a new factor column \code{hbf_outlier} with levels \code{"valid"},
\code{"missing"}, \code{"non_positive"}, \code{"above_threshold"}. \code{"missing"} marks
rows where \code{hbf_col} was already NA on input -- distinct from
\code{"valid"}, which means a value was recorded and is plausible.
}
\description{
Longline-specific. Converts HBF values outside the plausible range --
non-positive, or above \code{threshold} -- to NA, and adds a factor column
\code{hbf_outlier} recording \emph{why} each row was flagged. Values are not
imputed or otherwise corrected: only \code{hbf_col} is blanked, rows are
kept, so other catch/effort fields on the same row stay usable and
downstream analyses needing \code{hbf} handle the missingness explicitly.
}
\seealso{
Other cleaning steps:
\code{\link[=apply_pacific_mask]{apply_pacific_mask()}},
\code{\link[=bin_to_1cm]{bin_to_1cm()}},
\code{\link[=compute_grid_center]{compute_grid_center()}},
\code{\link[=convert_school_letters]{convert_school_letters()}},
\code{\link[=customRound]{customRound()}},
\code{\link[=fill_missing_month]{fill_missing_month()}},
\code{\link[=filter_bounding_box]{filter_bounding_box()}},
\code{\link[=filter_valid_categories]{filter_valid_categories()}},
\code{\link[=floor_lf_lengths]{floor_lf_lengths()}},
\code{\link[=infer_len_code_from_trip]{infer_len_code_from_trip()}},
\code{\link[=process_catch_data]{process_catch_data()}},
\code{\link[=process_lf_data]{process_lf_data()}},
\code{\link[=remove_dummy_fisheries]{remove_dummy_fisheries()}},
\code{\link[=remove_duplicates]{remove_duplicates()}},
\code{\link[=remove_invalid]{remove_invalid()}},
\code{\link[=remove_outliers_sd]{remove_outliers_sd()}},
\code{\link[=remove_sparse_lf_samples]{remove_sparse_lf_samples()}},
\code{\link[=remove_zero_fisheries]{remove_zero_fisheries()}},
\code{\link[=standardize_length_code]{standardize_length_code()}},
\code{\link[=treat_school]{treat_school()}},
\code{\link[=trim_vesselname]{trim_vesselname()}}
}
\concept{cleaning steps}
