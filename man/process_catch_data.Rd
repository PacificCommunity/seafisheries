% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fisheries_data.R
\name{process_catch_data}
\alias{process_catch_data}
\title{Process raw catch/effort fisheries data into cleaned form}
\usage{
process_catch_data(
  df,
  required_cols,
  catch_cols,
  effort_col,
  lat_col,
  lon_col,
  pmask_lookup,
  effort_sd_n = 3,
  gear = NULL,
  hbf_col = "hbf",
  hbf_threshold = 50,
  school_col = "school",
  vesselname_col = NULL,
  return_dummy = FALSE
)
}
\arguments{
\item{df}{A dataframe, pre-renamed and coordinate-converted.}

\item{required_cols}{Character vector of columns checked for validity.}

\item{catch_cols}{Character vector of one or more catch column names,
summed when checking dummy/zero fisheries.}

\item{effort_col}{Character; name of the effort column.}

\item{lat_col, lon_col}{Character; names of the grid-center lat/lon columns.}

\item{pmask_lookup}{A lookup dataframe with columns \code{latCent}, \code{lonCent}.}

\item{effort_sd_n}{Numeric; SD cutoff for effort outlier removal. Default 3.}

\item{gear}{Character or NULL; "L" for longline, "S" for purse seine, or
NULL (default) to skip the gear-specific step entirely.}

\item{hbf_col}{Character; HBF column name, used when \code{gear = "L"}.
Default "hbf".}

\item{hbf_threshold}{Numeric; HBF outlier threshold, used when
\code{gear = "L"}. Default 50.}

\item{school_col}{Character; school column name, used when \code{gear = "S"}.
Default "school".}

\item{vesselname_col}{Character or NULL; name of the vessel name column,
trimmed before duplicate removal if supplied. Default NULL (skipped).}

\item{return_dummy}{Logical; if TRUE, returns a list with both the
cleaned data and the removed dummy-fishery rows, instead of just the
cleaned data. Default FALSE.}
}
\value{
If \code{return_dummy = FALSE} (default): the cleaned dataframe.
If \code{return_dummy = TRUE}: a list with elements \code{data} (cleaned
dataframe) and \code{dummy_fisheries} (the removed dummy-fishery rows).
}
\description{
Runs \code{\link[=remove_invalid]{remove_invalid()}}, an optional gear-specific step,
\code{\link[=remove_dummy_fisheries]{remove_dummy_fisheries()}}, \code{\link[=remove_zero_fisheries]{remove_zero_fisheries()}},
\code{\link[=remove_outliers_sd]{remove_outliers_sd()}}, an optional vessel-name trim,
\code{\link[=remove_duplicates]{remove_duplicates()}}, and \code{\link[=apply_pacific_mask]{apply_pacific_mask()}} in sequence. Each step
prints its own removal report as it runs.
}
\details{
Assumes \code{df} has already been renamed to standard column names, had
coordinates converted via \code{\link[=convert_coordinates_df]{convert_coordinates_df()}}, and had grid centers
computed via \code{\link[=roundTo.5]{roundTo.5()}}.

Gear-specific step: \code{gear = "L"} (longline) runs \code{\link[=treat_hbf]{treat_hbf()}};
\code{gear = "S"} (purse seine) runs \code{\link[=treat_school]{treat_school()}}. Leave \code{gear = NULL}
(the default) for gears such as pole-and-line that have neither field --
the step is skipped entirely rather than forcing a choice. This step runs
immediately after \code{\link[=remove_invalid]{remove_invalid()}} and before the catch/effort filters,
since the purse-seine ordering hasn't been validated against a real
pipeline yet -- flag if that placement turns out to be wrong for your data.

Vessel-name trimming runs only if \code{vesselname_col} is supplied (not all
gears have this field, e.g. purse seine per your data), and always runs
before \code{\link[=remove_duplicates]{remove_duplicates()}} -- trimming after dedup would miss duplicate
rows that differ only by trailing whitespace on the vessel name.

\code{catch_cols} is summed across all columns supplied when checking for
dummy/zero fisheries, so pass whichever species/catch columns are present
and relevant for your gear -- e.g. \verb{_n} (count) columns for longline where
available, \verb{_w} (weight) columns for purse seine where \verb{_n} isn't present.
}
\seealso{
Other cleaning steps:
\code{\link[=apply_pacific_mask]{apply_pacific_mask()}},
\code{\link[=remove_dummy_fisheries]{remove_dummy_fisheries()}},
\code{\link[=remove_duplicates]{remove_duplicates()}},
\code{\link[=remove_invalid]{remove_invalid()}},
\code{\link[=remove_outliers_sd]{remove_outliers_sd()}},
\code{\link[=remove_zero_fisheries]{remove_zero_fisheries()}},
\code{\link[=treat_hbf]{treat_hbf()}},
\code{\link[=treat_school]{treat_school()}},
\code{\link[=trim_vesselname]{trim_vesselname()}}
}
\concept{cleaning steps}
