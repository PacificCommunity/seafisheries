% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fisheries_data.R
\name{hampel_LF_batch}
\alias{hampel_LF_batch}
\title{Hampel filter for length-frequency mean-length observations, applied per region}
\usage{
hampel_LF_batch(df, month_window, k, mad_scale = 1.4826, what = "mean_len")
}
\arguments{
\item{df}{A dataframe with columns \code{region}, \code{date}, and whatever \code{what}
names (same as \code{ref_data} inside \code{\link[=apply_hampel]{apply_hampel()}}).}

\item{month_window}{Numeric; +/- months defining the time window.}

\item{k}{Numeric; number of (scaled) MADs defining the outlier threshold.}

\item{mad_scale}{Numeric; MAD-to-SD scaling constant. Default 1.4826
(standard constant for consistency with a normal distribution).}

\item{what}{Character; name of the column in \code{df} to test for outliers.
Default "mean_len".}
}
\value{
\code{df} with \code{outlier} (logical), \code{n_points} (reference points
found), and \code{mad} (scaled MAD used) columns added.
}
\description{
For each region x date observation, compares its \code{what} value against the
median/MAD of same-region values within \code{month_window} months of it,
among the rows in \code{df}, and flags it as an outlier if it exceeds
\verb{median + k * (mad_scale-weighted MAD)}. Only rows with at least 3
reference points (same region, within the time window) get a computed
flag; others pass through unchanged with \code{n_points}/\code{mad} left NA. Rows
with \code{what <= 0} are skipped (never flagged, \code{n_points}/\code{mad} left NA),
matching the original per-row behavior.
}
\details{
Precomputes the region subset once per unique \code{region} in \code{df}, then
reuses that (much smaller) subset for every date query within that
region -- same precompute-once-per-group pattern as
\code{\link[=hampel_CE_batch]{hampel_CE_batch()}}, replacing what was previously a per-row
\code{dplyr::filter()} rescan of the full \code{ref_data} table. Same
self-inclusion note as \code{\link[=hampel_CE_batch]{hampel_CE_batch()}}: the reference subset
includes the observation itself.
}
\seealso{
Other hampel filter:
\code{\link[=apply_hampel]{apply_hampel()}},
\code{\link[=calculate_distance]{calculate_distance()}},
\code{\link[=hampel_CE_batch]{hampel_CE_batch()}}
}
\concept{hampel filter}
