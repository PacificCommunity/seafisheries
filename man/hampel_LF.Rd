% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fisheries_data.R
\name{hampel_LF}
\alias{hampel_LF}
\title{Hampel filter for a single length-frequency mean-length observation}
\usage{
hampel_LF(
  date_,
  reg,
  value,
  month_window,
  k,
  mad_scale,
  ref_data,
  what = "mean_len"
)
}
\arguments{
\item{date_}{Date; date of the observation.}

\item{reg}{Integer/character; region identifier.}

\item{value}{Numeric; mean length value being tested.}

\item{month_window}{Numeric; +/- months defining the time window.}

\item{k}{Numeric; number of (scaled) MADs defining the outlier threshold.}

\item{mad_scale}{Numeric; MAD-to-SD scaling constant.}

\item{ref_data}{A dataframe with columns \code{date}, \code{region}, and whatever
\code{what} names.}

\item{what}{Character; name of the column in \code{ref_data} to compare
\code{value} against. Default "mean_len".}
}
\value{
A list with \code{outlier} (logical), \code{n_points} (reference points
found), and \code{mad} (scaled MAD used).
}
\description{
Internal worker for \code{\link[=apply_hampel]{apply_hampel()}}, called once per (region, date) via
\code{pmap()}. Flags \code{value} (mean length) as an outlier if it's more than
\code{k} scaled-MADs from the median of same-region values within
\code{month_window} months, in \code{ref_data}. Same self-inclusion note as
\code{\link[=hampel_CE_batch]{hampel_CE_batch()}}: \code{ref_data} includes the observation itself.
}
\seealso{
Other hampel filter:
\code{\link[=apply_hampel]{apply_hampel()}},
\code{\link[=calculate_distance]{calculate_distance()}},
\code{\link[=hampel_CE_batch]{hampel_CE_batch()}}
}
\concept{hampel filter}
